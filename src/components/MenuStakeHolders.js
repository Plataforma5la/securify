import React from 'react';
import {
  StyleSheet,
  View,
  Text,
} from 'react-native';

class MenuStakeHolders extends React.Component{
  render(){
    return(
      // {
      // props.contactos.map((contacts, i) => {
      //   <Text>  {contacts.data[i].name}  </Text>
      // }
    )
  };
};

export default ContactosContainer
